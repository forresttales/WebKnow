
<style>
	
		.jqmWindow-6 {
		    display: none;
		    position: fixed;
		    left: 50%;
		    margin-left: -350px;
		    width: 700px;
			height: 700px;		    
		    background-color: #EEE;
		    color: #333;
		    border: 1px solid black;
		    padding: 0px;
		}		
	
		.jqmWindow-7 {
		    display: none;
		    position: fixed;
		    left: 50%;
		    margin-left: -350px;
		    width: 900px;
			height: 660px;		    
		    background-color: #EEE;
		    color: #333;
		    border: 1px solid black;
		    padding: 0px;
		}		
	
</style>

								<div class="jqmWindow jqmWindow-7" id="dialog_7">
							   		<div class="table-dl-form-2">
							   			<div class="row">
							   				<div class="cell-100percent">
							   					<div id="close_form7" class="form-edit-close jqmClose tip-left-center" tabindex="1">
							   						<img src="/assets/x_close_w20_h20.png" alt="X" height="20px">
							   					</div>
								                <div class="tip-content">
								                    <div class="tooltiptext">Close</div>
								                </div>																															   												   					
							   					<div style="clear: both;"></div>
							   				</div>
					   					</div>
									</div>				   				
									<div class="pad-left-20">
										<% if @publisher_user_logo_image %>
											<div style="display: inline-block;">
												<%= image_tag @publisher_user_logo_image.image_url(:user_600_600), id: "cropbox_user_logo" %>
											</div>
											<div style="display: inline-block; vertical-align: top;">
												<h4>Preview</h4>
												<div style="width:200px; height:200px; overflow:hidden">
												  <%= image_tag @publisher_user_logo_image.image_url(:user_600_600), :id => "preview_user_logo_200" %>
												</div>
												<div style="height: 4px"></div>
												<div style="width:100px; height:100px; overflow:hidden">
												  <%= image_tag @publisher_user_logo_image.image_url(:user_600_600), :id => "preview_user_logo_100" %>
												</div>
												<!-- <div style="height: 4px"></div>
												<div style="width:50px; height:50px; overflow:hidden">
												  <#%= image_tag @publisher_user_logo_image.image_url(:user_600_600), :id => "preview_user_50" %#>
												</div>
												<div style="height: 4px"></div>
												<div style="width:34px; height:34px; overflow:hidden">
												  <#%= image_tag @publisher_user_logo_image.image_url(:user_600_600), :id => "preview_user_34" %#>
												</div>
												<div style="height: 4px"></div> -->
												<%= form_tag( crop_commit_publisher_user_logo_images_path ) do %>
												  	<%= text_field_tag :crop_x, params["crop_x"], id: "id_logo_crop_x", class: "input-crop" %><br>
												  	<%= text_field_tag :crop_y, params["crop_y"], id: "id_logo_crop_y", class: "input-crop" %><br>
												  	<%= text_field_tag :crop_w, params["crop_w"], id: "id_logo_crop_w", class: "input-crop" %><br>
												  	<%= text_field_tag :crop_h, params["crop_h"], id: "id_logo_crop_h", class: "input-crop" %><br>
													<%= hidden_field_tag "image_id", @publisher_user_logo_image.id %>
												    <%= submit_tag "Save", class: '' %>
												<% end %>	
											</div>
										<% else %>
											<div> no @publisher_user_logo_image</div>
										<% end %>	
									</div>
								</div>
								
								<div class="jqmWindow jqmWindow-6" id="dialog_6">
							   		<div class="table-dl-form">
							   			<div class="row">
							   				<div class="cell-100percent">
							   					<div id="close_form6" class="form-edit-close jqmClose tip-left-center" tabindex="">
							   						<img src="/assets/x_close_w20_h20.png" alt="X" height="20px">
							   					</div>
								                <div class="tip-content">
								                    <div class="tooltiptext">Close</div>
								                </div>																															   					
							   					<div style="clear: both;"></div>
							   				</div>
					   					</div>
									</div>				   				
									<div class="pad-left-20">
										<div style="height: 20px;"></div>
										<div class="profile-grid profile-cs-style-3">
											<figure class="" id="profile_figure_logo_dialog">
												<img alt="photo not appearing" src="" />
												<figcaption class="figcaption-2">
													<div class="tip-top-center"></div>
									                <div class="tip-content">
									                    <div class="tooltiptext">Delete</div>
									                </div>																								
												</figcaption>
											</figure>
										</div>
									</div>
								</div>
								

								<% if @publisher_user_logo_image %>   				
								
									<div class="profile-grid profile-cs-style-3">
										<figure id="profile_figure" class="profile-figure dialog-6-figure">
										<!-- <figure id="profile_figure"> -->
											<img id="id_image" alt="photo not appearing" src="<%= @publisher_user_logo_image.image_url(:user_200_200) if @publisher_user_logo_image.image? %>" />
											<input class="x-container-hidden" type="hidden" value="<%= @publisher_user_logo_image.image_url(:user_600_600) if @publisher_user_logo_image.image? %>" />
											<figcaption class="figcaption-2">
												<div class="tip-top-center"><%= button_to "", destroy_publisher_user_logo_image_publisher_users_path(:id => @publisher_user_logo_image.id), :confirm => 'Delete this photo?', :remote => true, class: 'x-container' %></div>
								                <div class="tip-content">
								                    <div class="tooltiptext">Delete</div>
								                </div>																								
											</figcaption>
											<figcaption class="figcaption-1">
												<span class="dialog-7 label-dialog-figcaption">Crop/Center small images</span>
											</figcaption>
										</figure>
									</div>

								<% else %>

									<div class="profile-grid profile-cs-style-3">
										<figure id="profile_figure_logo_generic" class="profile-figure-logo-generic">
											<!-- <img alt="logo not appearing" src="../images_avatar/avatar_generic_1_w250_h310.png" /> -->
											<img alt="logo not appearing" src="../images_avatar/logo_generic_1_w200_h200.png" />
											<figcaption class="figcaption-1">
												<span class="dialog-8 label-dialog-figcaption">Add a photo</span>
											</figcaption>
										</figure>
									</div>
								
								<% end %>


	<script>

        function showPreviewLogo(coords)
        {
            if (parseInt(coords.w) > 0)
            {
                // var rx = 200 / coords.w;
                // var ry = 200 / coords.h;
		    	$('#id_logo_crop_x').val(coords.x);
		    	$('#id_logo_crop_y').val(coords.y);
		    	$('#id_logo_crop_w').val(coords.w);
		    	$('#id_logo_crop_h').val(coords.h);

                jQuery('#preview_user_logo_200').css({
					width: Math.round(200 / coords.w * $('#cropbox_user_logo').width()) + 'px',
				    height: Math.round(200 / coords.h * $('#cropbox_user_logo').height()) + 'px',
				    marginLeft: '-' + Math.round(200 / coords.w * coords.x) + 'px',
				    marginTop: '-' + Math.round(200 / coords.h * coords.y) + 'px'
                    // width: Math.round(rx * 200) + 'px',
                    // height: Math.round(ry * 200) + 'px',
                    // marginLeft: '-' + Math.round(rx * coords.x) + 'px',
                    // marginTop: '-' + Math.round(ry * coords.y) + 'px'
                });
                jQuery('#preview_user_logo_100').css({
					width: Math.round(100 / coords.w * $('#cropbox_user_logo').width()) + 'px',
				    height: Math.round(100 / coords.h * $('#cropbox_user_logo').height()) + 'px',
				    marginLeft: '-' + Math.round(100 / coords.w * coords.x) + 'px',
				    marginTop: '-' + Math.round(100 / coords.h * coords.y) + 'px'
                });
                // jQuery('#preview_user_50').css({
					// width: Math.round(50 / coords.w * $('#cropbox_user').width()) + 'px',
				    // height: Math.round(50 / coords.h * $('#cropbox_user').height()) + 'px',
				    // marginLeft: '-' + Math.round(50 / coords.w * coords.x) + 'px',
				    // marginTop: '-' + Math.round(50 / coords.h * coords.y) + 'px'
                // });
                // jQuery('#preview_user_34').css({
					// width: Math.round(34 / coords.w * $('#cropbox_user').width()) + 'px',
				    // height: Math.round(34 / coords.h * $('#cropbox_user').height()) + 'px',
				    // marginLeft: '-' + Math.round(34 / coords.w * coords.x) + 'px',
				    // marginTop: '-' + Math.round(34 / coords.h * coords.y) + 'px'
                // });
                
            }
        }
        
		// jQuery(window).load(function(){
			// jQuery("#cropbox_profile").each(function() {
			  // // image_to_crop = $(this);
			  // image_to_crop = $("#cropbox_profile");
			  // image_to_crop.Jcrop({
	          	// aspectRatio: 1,
	        	// setSelect: [0, 0, 600, 600],
	            // onSelect: function(coords){showPreview(image_to_crop, coords);},
			    // onChange: function(coords){showPreview(image_to_crop, coords);}
			// }
        // });
		
		// jQuery(window).load(function(){
		jQuery(document).ready(function() {
			image_to_crop_logo = $("#cropbox_user_logo");
			image_to_crop_logo.Jcrop({
            // jQuery('#cropbox_profile').Jcrop({
                aspectRatio: 1,
            	setSelect: [0, 0, 600, 600],
	            onSelect: function(coords){showPreviewLogo(coords);},
			    onChange: function(coords){showPreviewLogo(coords);}
	            // onSelect: function(coords){showPreview(image_to_crop, coords);},
			    // onChange: function(coords){showPreview(image_to_crop, coords);}
                // onSelect: showPreview,
                // onChange: showPreview                
            });
        });

        		
	</script>


	<script>

		// $(document).on('click','.dialog-2', {} ,function(e){
			// alert('crop');
			// $("#dialog_2").jqmShow();
		// });	


		function closeShowLogoPhoto() {
			$("#close_form5").trigger('click');
		};	  
		function closeAddLogoImage() {
			$("#close_form6").trigger('click');
		};	  
		function closeCropLogoImage() {
			$("#close_form7").trigger('click');
		};	  

		// $(window).load( function() {
		$(document).ready( function () {				
		
			// alert($("#profile_figure img").width());

			$("#profile_figure_logo_generic").on('click', function() {
				$(".dialog-8").click();	
			});
		
			var img_generic = $("#profile_figure_logo_generic img");
			$("<img/>")
	    		.attr("src", $(img_generic).attr("src"))
	    		.load(function() {
	        		img_width = this.width; 
	        		$("#profile_figure_logo_generic").css({
	        			width : this.width
	        	});
	    	});		

			var img = $("#profile_figure_logo img");
			$("<img/>")
	    		.attr("src", $(img).attr("src"))
	    		.load(function() {
	        		img_width = this.width; 
	        		$("#profile_figure_logo").css({
	        			width : this.width
	        	});	        		
	    	});		

		
			$(".dialog-7").on('click', function() {
				$("#dialog_7").jqmShow();
			});

			$(".dialog-8").on('click', function() {
				$("#dialog_8").jqmShow();
			});
		
			$('#dialog_8').jqm({
				onShow: function(hash) {
					hash.o.prependTo('body');
					hash.w.css('top', '-740px');
					hash.w.css('opacity', 1.0).fadeIn(200);
					// hash.w.show();
					$('#dialog_8').animate(
						{bottom:'-=800', top:'+=800'}, 
						500, 
						'easeInOutCirc', 
						function() {
							$('#dialog_8').animate(
								{bottom:'+=4', top:'-=4'}, 
								100, 
								'easeInBounce', 
								function() {
								}
							);
							// $('#dl_email').focus();
						}
					);
				}
			}); 		
		
			$('#dialog_7').jqm({
				onShow: function(hash) {
					hash.o.prependTo('body');
					hash.w.css('top', '-740px');
					hash.w.css('opacity', 1.0).fadeIn(200);
					// hash.w.show();
					$('#dialog_7').animate(
						{bottom:'-=800', top:'+=800'}, 
						500, 
						'easeInOutCirc', 
						function() {
							$('#dialog_7').animate(
								{bottom:'+=4', top:'-=4'}, 
								100, 
								'easeInBounce', 
								function() {
								}
							);
							// $('#dl_email').focus();
						}
					);
				}
			}); 		
			
			
			
			
			$(".dialog-6-figure").on('click', function(e) {
				if ( e.target.nodeName == "IMG" ) {
					var this_src = $(this).find("input.x-container-hidden").attr("value");
					$("#profile_figure_logo_dialog").find("img").attr("src", this_src );
					var this_delete = $(this).find("div.tip-top-center").html();
					$("#profile_figure_logo_dialog").find("div.tip-top-center").html(this_delete);
					$("#dialog_6").jqmShow();	
				}
			});
			$('#dialog_6').jqm({
				onShow: function(hash) {
					hash.o.prependTo('body');
					hash.w.css('top', '60px');
					hash.w.css('opacity', 1.0).fadeIn(200);
					var img = $("#profile_figure_logo_dialog img");
					$("<img/>")
			    		.attr("src", $(img).attr("src"))
			    		.load(function() {
			        		img_width = this.width; 
			        		$("#profile_figure_logo_dialog").css({
			        			width : this.width
			        	});	        		
			    	});		
				}
			}); 		
			
			
			
            $('.tip').each(function() {
                var tipContent = $(this).next('.tip-content').html();
                $(this).qtip({
                	style: { classes: 'tooltip-text-1' },
                    content: tipContent,
                    show: 'mouseover',
                    hide: 'mouseout'
                })
            });			
			
            $('.tip-bottom-center').each(function() {
                var tipContent = $(this).next('.tip-content').html();
                $(this).qtip({
                	style: { classes: 'tooltip-text-1' },
                    content: tipContent,
                    show: 'mouseover',
                    hide: 'mouseout',
				    position: {
				        my: 'top center',
				        at: 'bottom center'
				        // target: $('.tip-bottom-center')
    				}                    
                })
            });			

            $('.tip-top-center').each(function() {
                var tipContent = $(this).next('.tip-content').html();
                $(this).qtip({
                	style: { classes: 'tooltip-text-1' },
                    content: tipContent,
                    show: 'mouseover',
                    hide: 'mouseout',
				    position: {
				        my: 'bottom center',
				        at: 'top center'
				        // target: $('.tip-top-center')
    				}                    
                })
            });			
			
            $('.tip-left-center').each(function() {
                var tipContent = $(this).next('.tip-content').html();
                $(this).qtip({
                	style: { classes: 'tooltip-text-1' },
                    content: tipContent,
                    show: 'mouseover',
                    hide: 'mouseout',
				    position: {
				        my: 'right top',
				        at: 'left center'
				        // target: $('.tip-top-center')
    				}                    
                })
            });			
		
		});
	  

	</script>		

