<% unless current_user?(@user) %>
  <div id="follow_form">
	  <% if current_user.following?(@user) %>
	    <%= render 'user/users/unfollow' %>
	  <% else %>
	    <%= render 'user/users/follow' %>
	  <% end %>
  </div>
<% end %>
