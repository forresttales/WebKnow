


	<style>
		
		.underline-none {
			text-decoration: none;
		}
		.underline-none:hover {
			text-decoration: none;
		}
		
	</style>



	<% @b_has_like = post_user_has_like_for(@post_user) %>
	<div class="table">
		<div class="row">
			<!-- <div class="cell-30 ks-tip-container"> -->
			<div class="cell-30 ks-tip-container">
				
				<% if false %>
				<div id="" class="ks-tip-target">
				<% if @b_has_like %>
					<div class="btn-submit-post-user-like-delete" name="<%= @nlist %>">
						<input type="hidden" name="post_user_id_hidden" value="<%= @post_user.id %>">
						<div class="icon-unlike"></div>
					</div>
				<% else %>
					<div class="btn-submit-post-user-like-create" name="<%= @nlist %>">
						<input type="hidden" name="post_user_id_hidden" value="<%= @post_user.id %>">
						<div class="icon-like"></div>
					</div>
				<% end %>																
				</div>
			    <div class="ks-tip-1 ks-tip-34minus">
			    	<div class="ks-tip-1-indicator"></div>
					<% if @b_has_like %>
						Unlike this
					<% else %>
						Like this
					<% end %>
			    </div>
			    <% end %>
			    
			</div>
			<div class="cell-100">
				<% if @post_user.post_user_likes.count == 0 %>				
				<div class="">
					<span class="post-user-span-3">Likes <%= @post_user.post_user_likes.count %></span>					
				<% else %>
				<div class="dialog-101-figure">
					<span class="post-user-span-2">Likes <%= @post_user.post_user_likes.count %></span>					
				<% end %>

					<div id="dialog_101_content_<%= @nlist %>" class="dialog-101-content hidden">					

						<% cnt = 0 %>
						<% post_user_likes = @post_user.post_user_likes 	%>
						<div class="" style="margin: 0px; padding: 0 0 0 34px;">
						<% post_user_likes.each do |post_user_like|    		%>
						<% 		user_id = post_user_like.user_id 	   		%>
						<% 		user = User.find_by_id(user_id)		   		%>
						<%		if !user.nil?								%>
						<% 			cnt += 1								%>
						<%			if cnt == 1								%>			
						<%			end										%>
						<%			user_image_primary = user.user_images.where( :primary => true ).first 	%>
								<div class="" style="display: inline-block; width: 100px; height: 120px; margin: 4px; float: left; position: relative;">
									<a href="<%= user.slug %>" class="underline-none" style="width: 100px; height: 120px;">
										<div style="margin: 0; padding: 0; width: 100px; height: 100px;">
						<% 			if !user_image_primary.nil? 											%>
											<img alt="" src="<%= user_image_primary.image_url(:user_100_100) if user_image_primary.image? %>" width="100px" height="100px" style="margin: 0;" />
						<%			else																	%>
											<img alt="" src="../images_avatar/avatar_generic_1_w100_h100.png" width="100px" height="100px" style="margin: 0;" />				
						<% 			end										%>
										</div>
										<div class="" style="width: 100%; height: 14px; margin: 0; padding: 0;">
											<!-- <span class="dialog-span-1"><#%= user.name_first %#> </span> -->
											<span class="dialog-span-1" style="margin: 0; vertical-align: top;"><%= user.name_last %></span>
										</div>											
									</a>
								</div>
						<%			if cnt == 4								%>
						<%				cnt = 0 							%>			
								<div></div>		
						<%			end										%>
						<%		end											%>
						<% end 												%>
						</div>
						<div style="clear: both;"></div>
					</div>
					
				</div>
			</div>
		</div>
	</div>

	<script>
		
		// var scroll_top = 0;
		
		$(document).ready(function() {			
      	// $(function() {


			$(".dialog-101-figure").on('click', function() {
				var id = $(this).find('.dialog-101-content').attr('id');
				$(document).trigger('click_101', [id]);
			});


			// $(".dialog-101-figure").on('click', function(e) {
					// // scroll_top = $(window).scrollTop();
					// // scroll_top = $(window).scrollTop();
					// // $(document).trigger('getScroll');
					// scroll_top = getScrollPos();
				// // if ( e.target.nodeName == "IMG" ) {
					// var this_src = $(this).find("input.container-image-hidden").attr("value");
					// $("#profile_figure_dialog_101").find("img").attr("src", this_src );
					// var this_delete = $(this).find("div.tip-top-center").html();
					// $("#profile_figure_dialog_101").find("div.tip-top-center").html(this_delete);
					// $("#dialog_101").jqmShow();	
				// // }
			// });
			// $('#dialog_101').jqm({
				// onShow: function(hash) {
					// hash.o.prependTo('body');
					// // hash.w.css('top', 0);
					// hash.w.css('top', scroll_top);
					// hash.w.css('opacity', 1.0).fadeIn(200);
					// var img = $("#profile_figure_dialog_101 img");
					// $("<img/>")
			    		// .attr("src", $(img).attr("src"))
			    		// .load(function() {
			        		// img_width = this.width; 
			        		// $("#profile_figure_dialog_101").css({
			        			// // width : this.width 
			        	// });	        		
			    	// });		
				// }
			// }); 		


		});
		
	</script>
