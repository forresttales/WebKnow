

	<!-- <style>

		/*input[type=file] {*/
		.input-file {
			font-family: Arial, Helvetica, sans-serif;
			font-size: 14px;
			color: #888;
			background-color: #fff;
			border: 1px solid #ccc;			
			width: 550px;
			height: 30px;
		}
		.btn-submit {
		  	color: #0FEAFF;
		  	display: inline-block;
		  	text-decoration: none; 
		  	width: 308px;
		  	height: 30px;
		  	background: linear-gradient(#000, #000);
		  	text-shadow: 1px 1px 1px #0E86B2; 
		  	border-radius: 6px;
		}
		.btn-submit:focus {
		  background: #CCC; 
		  color: #5F7A2A;
		  box-shadow: inset 0 1px 0 rgba(200,200,200,0.2);
		}
		.btn-submit:hover { 
		  box-shadow: inset 0 1px 1px rgba(255,255,255,0.2), 
		  inset 0 1.5em 1em rgba(255,255,255,0.3);
		}
		.btn-submit:active { 
		  box-shadow: inset 0 1px 1px rgba(255,255,255,0.2), 
		  inset 0 1.5em 1em rgba(0,0,0,0.3); 
		}
		
	</style> -->	


	<%= form_for PublisherProductCorporateLogo.new, :url => upload_corporate_logo_publisher_product_descriptions_path(@publisher_product_corporate_logo_new), :html => {:multipart => true}, :remote => true do |f| %>
	  <p><%= f.file_field :image %></p>
	  <%= f.hidden_field 'publisher_id', :value => @publisher_id %>
	  <%= f.hidden_field 'publisher_product_id', :value => @publisher_product_id %>
	  <p><%= f.submit :class => 'btn-submit', :value => 'Upload product corporate logo' %></p>
	<% end %>


	<script>
	
		// $(window).load( function() {
		$(document).ready(function(){
			
		});
						
	</script>
