$("#user-id-<%= @new_friend.id %>").replaceWith("<%= j link_to 'Remove Friend', {:action => 'remove_friend', :friend_id => @new_friend.id}, :remote => true, :method => :post, :id => 'user-id-' + @new_friend.id.to_s %>");